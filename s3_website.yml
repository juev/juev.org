s3_id: <%= ENV['s3_id'] %>
s3_secret: <%= ENV['s3_secret'] %>
s3_bucket: <%= ENV['s3_bucket'] %>

site: public

max_age: 0
cache_control: no-cache, no-store

gzip: false

s3_endpoint: eu-west-1

redirects:
  rss/: atom.xml
